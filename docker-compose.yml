services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cfd}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-cfd_remote_assist}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cfd}"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 3000
      NODE_ENV: production
      DATABASE_URL: postgresql://${POSTGRES_USER:-cfd}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-cfd_remote_assist}
      OIDC_ISSUER: ${OIDC_ISSUER:?Set OIDC_ISSUER}
      OIDC_AUDIENCE: ${OIDC_AUDIENCE:-cfd-remote-assist}
      OIDC_JWKS_URI: ${OIDC_JWKS_URI:-}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://localhost}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:?Set PUBLIC_BASE_URL}
      PHONEDB_TLS_INSECURE: ${PHONEDB_TLS_INSECURE:-true}
      GITHUB_APK_REPO: ${GITHUB_APK_REPO:-cfd2474/EUD_Remote_Assist_Portal}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      PORTAL_SETTINGS_DIR: /app/data
    volumes:
      - ./data:/app/data
    expose:
      - "3000"

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
      args:
        VITE_OIDC_AUTHORITY: ${OIDC_ISSUER}
        VITE_OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-cfd-remote-assist}
        VITE_API_BASE: ""
        VITE_WS_BASE: ""
    restart: unless-stopped
    expose:
      - "80"

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    depends_on:
      - server
      - web
    ports:
      # NGINX_BIND_ADDR empty → all interfaces (standalone). Set 127.0.0.1 for loopback-only (infra-TAK / host TLS).
      - "${NGINX_BIND_ADDR:-}:${HTTP_PORT:-80}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro

volumes:
  postgres_data:
